version: '2'
services:
  gpi:
    image: docker-int.symphonyretailai.com/tools/gpi:latest
    ports:
      - 8081:8080
    volumes:
      - GPI_DATA_QA:/usr/local/tomee/GPI_DATA

volumes:
  GPI_DATA_QA:
